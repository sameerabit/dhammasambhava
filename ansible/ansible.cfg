[defaults]
inventory = inventory.yml
host_key_checking = False
remote_user = root
private_key_file = ~/.ssh/id_rsa_personal
retry_files_enabled = False
timeout = 30

[ssh_connection]
pipelining = True
ssh_args = -o ControlMaster=auto -o ControlPersist=60s -o StrictHostKeyChecking=no
